/**
 * FileGenerator Page
 * Protected route wrapper for file generation
 */

import React from 'react';
import FileGenerator from '../components/FileGenerator';

const FileGeneratorPage: React.FC = () => {
  return (
    <div className="w-full">
      <FileGenerator />
    </div>
  );
};

export default FileGeneratorPage;
